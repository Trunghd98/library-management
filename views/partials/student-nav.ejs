<nav class="navbar navbar-expand-lg navbar-dark bg-success shadow-sm">
  <div class="container">
    <a class="navbar-brand fw-bold fs-6" href="/student">
      <i class="bi bi-book-half me-2"></i><%= libraryName %>
    </a>
    <div class="ms-auto d-flex align-items-center gap-2">
      <a href="/student/notifications" class="btn btn-outline-light btn-sm position-relative">
        <i class="bi bi-bell"></i>
        <% if (typeof unreadCount !== 'undefined' && unreadCount > 0) { %>
        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
          <%= unreadCount %>
        </span>
        <% } %>
      </a>
      <span class="text-white-50 small"><i class="bi bi-person-circle me-1"></i><%= user.fullName %></span>
      <a href="/logout" class="btn btn-outline-light btn-sm">
        <i class="bi bi-box-arrow-right"></i>
      </a>
    </div>
    <div class="collapse navbar-collapse mt-2 mt-lg-0" id="studentNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link text-white" href="/student"><i class="bi bi-house me-1"></i>Trang chủ</a></li>
        <li class="nav-item"><a class="nav-link text-white" href="/student/books"><i class="bi bi-search me-1"></i>Tra cứu sách</a></li>
        <li class="nav-item"><a class="nav-link text-white" href="/student/my-borrowings"><i class="bi bi-clock-history me-1"></i>Lịch sử mượn</a></li>
      </ul>
    </div>
  </div>
</nav>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
